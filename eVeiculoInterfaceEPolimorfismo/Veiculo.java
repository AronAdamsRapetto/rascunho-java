package eVeiculoInterfaceEPolimorfismo;

/*
 * Uma interface não pode ser implementada nem instanciada, ela serve apenas como um contrato ou modelo de
 * como uma classe deve ser criada, ou seja, na interface deve conter os métodos e/ou atributos
 * obrigatórios da classe.
 */

public interface Veiculo {
  void acelerar();
  void frear();
}
